// -- Model dump before bytecode pipeline
module 0x8675309::M {
    struct S {
        f: u64,
    }
    private fun t0(s: &M::S,s_mut: &mut M::S): (u64, u64) {
        Tuple(select M::S.f<&M::S>(s), select M::S.f<&mut M::S>(s_mut))
    }
} // end 0x8675309::M

// -- Sourcified model before bytecode pipeline
module 0x8675309::M {
    struct S {
        f: u64,
    }
    fun t0(s: &S, s_mut: &mut S): (u64, u64) {
        (s.f, s_mut.f)
    }
}
